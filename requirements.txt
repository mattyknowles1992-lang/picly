# Core API packages
flask==3.0.0
flask-cors==4.0.0
requests==2.31.0
python-dotenv==1.0.0
gunicorn==21.2.0

# Payment Processing
stripe==7.8.0

# Note: Pillow pinned for Python 3.13 compatibility
# Note: replicate and openai libraries removed - using REST APIs directly instead

# Task scheduling
schedule

# Imaging library
Pillow>=11.0.0,<12
