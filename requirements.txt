# Core API packages
flask==3.0.0
flask-cors==4.0.0
requests==2.31.0
python-dotenv==1.0.0
gunicorn==21.2.0

# Payment Processing
stripe==7.8.0

# Note: Pillow removed - image enhancement disabled to avoid Python 3.13 compatibility issues
# Note: replicate and openai libraries removed - using REST APIs directly instead

# Task scheduling
schedule

# Task scheduling
schedule
